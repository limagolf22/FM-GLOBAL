/**
 * The complete list of data providers that can be selected for flight data input.
 */

const dataProviders = {
    FS: 'Flight Simulator',
    XPLANE: 'X-Plane',
    GPS: 'GPS',
};

export default dataProviders;